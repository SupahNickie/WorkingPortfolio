<!DOCTYPE html>
<html>

<%= render 'layouts/header' %>

<body class="<%= @seattle ? 'seattle' : ''%>">

<%= render 'layouts/nav' %>

<div class="container" %>
<% flash.each do |key, value| %>
  <h4><%= value %></h4>
<% end %>
  <%= yield %>
</div>

</body>
</html>
