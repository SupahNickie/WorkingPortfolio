<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<p>
  <strong>Technologies used:</strong>
  <%= @project.technologies_used %>
</p>

<p>
  <strong>Body:</strong>
  <%= @project.body %>
</p>

<%= image_tag @project.photo.url(:full) %>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
